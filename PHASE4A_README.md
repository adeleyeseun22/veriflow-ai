# Phase 4A — Vector Embeddings and Semantic Retrieval

This package adds FastEmbed-based chunk embeddings, pgvector persistence, an HNSW cosine index,
and a workspace-scoped semantic-search endpoint. See `docs/semantic-retrieval.md` for architecture.
